{% extends 'base.html.twig' %}

{% block title %}Editeaza antrenamente{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles.css') }}">
    <style>
        :root {
            --background-image-url: url('{{ asset('images/HomepageBackground.jpg') }}');
        }
    </style>
{% endblock %}

{% block body %}

    <div class="form-container">
        <h1>Editeaza antrenament</h1>
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_end(form) }}
    </div>
{% endblock %}